# OS/editor noise
.DS_Store
__pycache__/
.idea/
.vscode/
*.swp
*.swo
*~
Thumbs.db

# Rust
target/
**/*.rs.bk
*.pdb

# Go
bin/
coverage.out
server/bin/
server/vendor/

# Build artifacts
# (The React/Vite frontend was removed; `dist/` is still used by the macOS
#  tray app build, and `node_modules/`/`build/`/`.cache/` are kept defensively.)
node_modules/
dist/
build/
.cache/

# Coverage/test output
coverage/
*.lcov
*.profraw

# Local document exports
*.doc
*.docx
*.rtf
*.odt
*.pages
*.xls
*.xlsx
*.ppt
*.pptx
*.odp
*.ods
~$*
.~lock.*#

# Local-only project notes and research drafts (kept on disk, never published)
P2PNet-Design.md
PRODUCT.md
research/nat-traversal-paper/

# Public docs and redacted deployment templates are tracked. Runtime values
# and generated evidence remain local.
docs/generated/
docs/adr/
deploy/staging/*.env
deploy/staging/*.env.local
deploy/staging/*.secret
!deploy/staging/*.env.example

# Test scripts that hardcode real infra/device IPs (kept on disk, never published)
scripts/mac-remote-smoke.sh
scripts/windows-remote-smoke.ps1
scripts/tun-ping-smoke.sh

# Local-only dual-end shell helpers; keep their Markdown runbooks publishable.
# Existing tracked scripts remain tracked until explicitly removed from Git.
scripts/dual-end/*.sh

# Release packages and archives
*.pdf
*.dmg
*.pkg
*.msi
*.msix
*.exe
*.AppImage
*.deb
*.rpm
*.zip
*.7z
*.rar
*.tar
*.tar.gz
*.tgz
*.tar.bz2
*.tar.xz
*.sha256
*.sig
*.backup
*.backup.enc
path-observability-evidence.json
artifacts/
/release/
releases/
dist-release/

# Logs/env
*.log
.env
.env.*

# Runtime/native artifacts
*.dll
*.db
*.sqlite
*.sqlite3
p2pnet-config.json
.temp/
*.tmp
*.bak
