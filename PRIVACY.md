# 隐私说明

P2WLAN 不在新安装中预置项目控制面或中继地址。使用自托管或其他管理员提供的服务时，数据由该服务的运营者按其策略处理。

## 客户端与 Control

为完成登录、设备注册、房间管理和信令，Control 可能保存账号标识、设备公钥与名称、虚拟地址、房间成员关系、短期凭据、撤权记录和最近在线信息。服务端 SQLite 是部署者负责保护的数据存储。

## Relay 与诊断

Relay 转发加密业务载荷，但仍可能看到节点标识、连接时间、方向、包大小和连接状态等元数据。诊断状态和日志可能包含版本、平台、网络类型、路径状态、错误码、虚拟地址和时间线；它们不应包含私钥、JWT、设备凭据或业务明文。

支持包上传必须由用户显式执行。上传前应检查内容、目标服务和保存期限；不需要上传时使用本地导出或取消上传。部署者应为 Control、Relay、日志和备份设置访问控制、保留期限与删除流程。

本说明描述当前实现边界，不承诺“完全不收集”或替代具体自托管服务的隐私政策。
