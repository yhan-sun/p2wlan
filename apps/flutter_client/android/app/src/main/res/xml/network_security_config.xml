<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The P1/P1.5 app is a read-only diagnostics viewer. The endpoint is
         user-configurable and commonly uses plain HTTP on localhost/LAN. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
